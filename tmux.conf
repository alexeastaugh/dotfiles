# Alex Eastaugh .tmux.conf

## RELOAD FILE AFTER ANY CHANGES
# From tmux session:
# C-a : source-file ~/.tmux.conf
# From a shell:
# $ tmux source-file ~/.tmux.conf

## KEYBINDINGS
# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix
